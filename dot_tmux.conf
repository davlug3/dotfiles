unbind r
bind r source-file ~/.tmux.conf
set -g default-terminal "xterm-256color"
setw -g mode-keys vi
bind-key h select-pane -L
bind-key j select-pane -D
bind-key k select-pane -U
bind-key l select-pane -R

set -g @tpm_plugins 'tpm'
if "test ! -d ~/.tmux/plugins/tpm" \
   "run 'git clone https://github.com/tmux-plugins/tpm ~/.tmux/plugins/tpm && ~/.tmux/plugins/tpm/bin/install_plugins'"

set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'christoomey/vim-tmux-navigator'
set -g @plugin 'dracula/tmux'


set -g @dracula-show-powerline true
set -g @dracula-show-flags true
set -g @dracula-day-month true
set -g @dracula-fixed-location "Manila"
set -g @dracula-plugins "weather"
set -g @dracula-show-left-icon session
set -g @dracula-left-icon-padding 1
set -g @dracula-battery-label "Battery"
set -g @dracula-border-contrast true
set -g @dracula-cpu-usage-label "CPU"
set -g status-position bottom

# Extended (kitty) keyboard protocol. Keep OFF: the client terminal only reports
# xterm-256color and does not implement CSI-u, which caused garbage hex to be
# echoed into panes. Re-enable only if the real client supports kitty protocol.
# set -g extended-keys on
# set -g extended-keys-format csi-u

run '~/.tmux/plugins/tpm/tpm'



# panes
set -g pane-border-style 'fg=red'
set -g pane-active-border-style 'fg=yellow'

# messages
set -g message-style 'fg=yellow bg=red bold'



